# Next.js Environment Variables
# Copy this file to .env.local and update the values

# Site URL used for OpenGraph metadataBase (no trailing slash)
# NEXT_PUBLIC_SITE_URL=https://airecap.news

# Optional API endpoint override for newsletter data.
# Leave unset when using `npm run dev` so same-origin `/.netlify/functions/*` is used.
# NEXT_PUBLIC_NEWSLETTER_URL=http://localhost:8888/.netlify/functions

# For production (uncomment and update)
# NEXT_PUBLIC_NEWSLETTER_URL=https://your-site.netlify.app/.netlify/functions
